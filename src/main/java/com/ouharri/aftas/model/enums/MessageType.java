package com.ouharri.aftas.model.enums;

/**
 * Enum representing different types of chat message content.
 *
 * <p>Each enum value corresponds to a specific type of message content, such as text, image, PDF, etc.</p>
 *
 * @version 1.0
 */
public enum MessageType {
    TEXT,
    IMAGE,
    PDF,
    VIDEO,
    AUDIO,
}
